using System;
using System.Threading;

namespace StackGame
{
    class Program
    {
        static void Main(string[] args)
        {

            Board board = new Board();
            //Block block = new Block(board);
            //block.MakeBlock();
            board.MakeBoard();
            //board.MakeBoard();
            //block.MoveBlock();
        }
    }
}
